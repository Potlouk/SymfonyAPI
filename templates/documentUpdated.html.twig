<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;500&display=swap" rel="stylesheet"
        type="text/css">
    <style>
        body {
            background-color: #F4F5FB;
            font-family: Montserrat, Helvetica, Arial, sans-serif;
            margin: 0;
            padding: 0;
        }

        .mb-4 {
            margin-bottom: 1rem;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            background: #F4F5FB;
            border-radius: 20px;
            padding: 20px;
            box-sizing: border-box;
        }

        .header {
            font-size: 20px;
            font-weight: 500;
            line-height: 30px;
            padding: 10px 0;
            text-align: left;
        }

        .body-text {
            font-size: 16px;
            font-weight: 500;
            line-height: 20px;
        }

        .button {
            display: inline-block;
            padding: 10px 20px;
            font-size: 16px;
            font-weight: 500;
            color: #ffffff;
            background-color: #006BFF;
            border-radius: 10px;
            text-decoration: none;
            text-align: center;
        }

        .footer {
            font-size: 16px;
            font-weight: 400;
            line-height: 20px;
            padding: 10px 0;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">{{ header }}</div>
        <div class="body-text">
            <div class="mb-4">
                {{ manager }} reopened your <strong>{{ type }}</strong>
                for <strong>{{ propertyName }}</strong>.
                you have <strong>{{ expireDays }}</strong> day(s) <strong>({{ expireDate }})</strong> to complete it.
            </div>
            <div class="mb-4">
                <div class="mb-4">Click the link below to update the <strong>{{ type }}</strong>.</div>
                <table role="presentation" border="0" cellpadding="0" cellspacing="0" class="button-box" style="margin: 0 auto;">
                    <tr>
                        <td align="center">
                            <a href="{{ link }}" target="_blank" class="button" style="color:#FFFFFF">Get Started</a>
                        </td>
                    </tr>
                </table>
            </div>
            <div class="mb-4">Please view tutorial on how to complete <strong>{{ type }}</strong> if needed.</div>
        </div>
        <div class="footer">{{ footer }}</div>
    </div>
</body>

</html>